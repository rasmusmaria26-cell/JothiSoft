# Dependencies
node_modules
.pnp
.pnp.js

# Next.js
apps/web/.next
apps/web/out

# FastAPI
apps/api/__pycache__
apps/api/*.pyc
apps/api/.venv
apps/api/venv

# Turbo
.turbo

# Env files — cover all variants, never commit secrets
.env
.env.*
!.env.example
apps/**/.env
apps/**/.env.*
!apps/**/.env.example

# Scratch / debug files — keep out of production builds
apps/api/src/scratch_*.ts
scripts/scratch_*.ts

# Build outputs
dist

# OS
.DS_Store
Thumbs.db

# IDE
.vscode
.idea

# ── Loose asset files dropped in repo ROOT only (not inside apps/) ────────────
/*.pdf
/*.png
/*.jpg
/*.jpeg
/*.gif

# ── Backup / temp editor files ───────────────────────────────────────────────
*.bak
*.tmp
*.orig
*.swp
*~

# ── API one-off test / scratch scripts ───────────────────────────────────────
apps/api/test_*.js
apps/api/test_*.ts
apps/api/src/scripts/

# ── Planning & AI-generated session artifacts (local only) ───────────────────
implementation_plan.md
walkthrough.md
task.md

# ── Scratch workspace directories ────────────────────────────────────────────
jothisoft/
